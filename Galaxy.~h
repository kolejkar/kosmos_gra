//---------------------------------------------------------------------------

#ifndef GalaxyH
#define GalaxyH

#include "Lisc.h"
#include "SelRegion.h"

//---------------------------------------------------------------------------
class Galaxy
{
private:
        star *idgw[1000];
public:
        object *maps[100][100];
        Galaxy();
        void LosujGalaxy();
        Region *GetRegion(int regX,int regY);
        bool HaveStar(int regX,int regY);
        void Czysc();
        ~Galaxy();
};
#endif
